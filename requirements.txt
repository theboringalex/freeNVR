fastapi>=0.111.0
uvicorn[standard]>=0.29.0
aiosqlite>=0.20.0
aiohttp>=3.9.0
pydantic>=2.7.0
python-multipart>=0.0.9
numpy>=1.26.0

# AI inference (optional — install one of these for Coral/TFLite detection)
# pip install ai-edge-litert          # Google's Python 3.11+ TFLite runtime (CPU + Coral via delegate)
# pip install tflite-runtime          # Legacy TFLite CPU runtime
# pip install pycoral                 # Official Coral library (Python <= 3.9 only)
#   --extra-index-url https://google-coral.github.io/py-repo/
